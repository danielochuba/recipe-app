<%= form_with(model: @recipe_food, url: update_user_recipe_recipe_foods_path(current_user, @recipe, @recipe_food), method: :patch) do |f| %>
  <%= f.label :quantity, 'New Quantity' %>
  <%= f.number_field :quantity, class: 'form-control' %>
  <%= f.submit 'Update Quantity', class: 'btn btn-sm btn-primary' %>
<% end %>
<turbo-frame id="recipe-food-form">
</turbo-frame>
